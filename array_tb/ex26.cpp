#include <bits/stdc++.h>
typedef long long ll;
using namespace std;

int main(){
    int n,x; cin >> n >> x;
    int a[n];
    for(int &x : a) cin >> x;
    ll ans = 0;
    for(int i = 0; i < n; i++){
        if(a[i] - a[i-1] != 1){
            
        }
    }
    
    return 0;
}