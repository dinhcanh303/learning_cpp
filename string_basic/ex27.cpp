#include <bits/stdc++.h>
using namespace std;

bool isValidEmail(string s){
    if(s.find('@gmail.com') == string::npos || 
    s.find('@28tech.com.vn') == string::npos ||
    s.find('@yahoo.com') == string::npos ||
    s.find('@hotmail.com') == string::npos)
        return false;
        
}
int main(){
    int n; cin >> n;
    string s;
    while(n--){
        cin >> s;

    }
    return 0;
}